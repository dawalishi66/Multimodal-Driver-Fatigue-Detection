# 贡献指南

## 开发流程

1. 从最新 `main` 创建短期任务分支，例如 `feat/fatigue-can-metadata`。
2. 一项分支只处理一个明确任务，不混入无关格式化或大规模重命名。
3. 修改前阅读 `docs/EXPERIMENT_RULES.md` 和 `docs/INTERFACES.md`。
4. 提交前运行 `python scripts/check_repository.py` 和 `python -m pytest`。
5. 推送分支并创建 PR；普通修改至少由一名非作者审核。
6. 公共标签、窗口、split、metadata、特征或模型接口的修改，必须由李坤洋和受影响模块负责人确认。

禁止直接覆盖 `main`、强制推送共享历史，或通过删除测试来让检查通过。

## 分支与提交

- `feat/<scope>`：功能或模型工作。
- `fix/<scope>`：错误修复。
- `docs/<scope>`：仅文档。
- `chore/<scope>`：工程维护。

提交信息使用 `type: summary`，例如 `feat: add fatigue CAN metadata builder`。

## PR 最低信息

- 本次目标、负责人、修改范围和公共接口影响。
- 输入、输出及版本。
- 实际运行过的命令、结果、失败项和剩余限制。
- 是否接触 test、真实数据、第三方代码或权重。

作者不能用“AI 生成”代替验证记录。未运行的命令和实验必须明确标注。

## 数据和大文件

不得提交原始或可识别个人的视频、音频与 CAN，完整特征、权重、checkpoint、完整运行包，以及本机绝对路径、令牌、密码和临时签名链接。

允许提交代码、机器无关配置、字段说明、小型合成测试、无隐私的汇总结果以及记录大小和 SHA-256 的大文件索引。

新增第三方代码前核对许可并保留来源。误提交数据或密钥后立即报告，不自行重写共享历史。

## 评审责任

- 单模态模块由对应负责人维护，公共组件由李坤洋维护。
- 李坤洋提交的公共组件由至少一名实际使用者审核。
- 饶棋涛引入 MulT 时，由李坤洋和至少一名上游模态负责人核对接口。
- CODEOWNERS 在团队 GitHub 用户名和仓库权限确定前不启用。
